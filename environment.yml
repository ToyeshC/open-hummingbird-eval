name: hbird
channels:
  - conda-forge
  - nvidia
  - pytorch
  - defaults

# # CUDA 12.4 wheels for torch/vision/audio
# variables:
#   PIP_EXTRA_INDEX_URL: https://download.pytorch.org/whl/cu124

dependencies:
  - python=3.11
  - typing-extensions>=4.8
  - pip
  - pip:
      # # PyTorch (CUDA 12.4)
      # - torch==2.6.0
      # - torchvision==0.21.0
      # - torchaudio==2.6.0

      # Lightning ecosystem
      - lightning==2.4.0
      - torchmetrics==1.7.0
      - pyyaml>=5.0

      # Utilities
      - tqdm==4.67.1
      - scipy==1.15.2
      - joblib==1.4.2
      - numpy==1.26.4
      # - triton==3.2.0  # PyTorch should bundle and pin Triton internally

      # Nearest neighbors
      - faiss-gpu-cu12
      - scann==1.3.5

      # Hugging Face
      - huggingface-hub>=0.16.4
      - transformers>=4.34.0

      # Optional performance (warning is shown if not installed)
      - xformers

      # # Radio model
      # - timm
      # - open_clip_torch
      # - einops

      # # TIPS model (be aware of TF/JAX compat notes below)
      # - tensorflow_text
      # - mediapy
      # - jax
      # - jaxlib
      # - scikit-learn

      # # Colmap / plots
      # - seaborn
      # - pandas
      # - jinja2

      # # VGGT
      # - git+https://github.com/facebookresearch/vggt.git
